/* Generated from ./4-param.cloog by CLooG 0.15 64 bits in 0.06s. */
for (i=p;i<=min(m-1,q);i++) {
  S2 ;
}
for (i=max(m,p);i<=min(n,q);i++) {
  S1 ;
  S2 ;
}
for (i=m;i<=min(n,p-1);i++) {
  S1 ;
}
for (i=max(max(m,p),q+1);i<=n;i++) {
  S1 ;
}
for (i=max(max(m,n+1),p);i<=q;i++) {
  S2 ;
}
