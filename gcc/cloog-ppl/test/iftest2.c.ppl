/* Generated from ./iftest2.cloog by CLooG 0.15 64 bits in 0.02s. */
if (M >= 1) {
  for (i=1;i<=N;i++) {
    for (j=1;j<=M;j++) {
      if (i <= 2*M) {
        S1 ;
      }
      if (i >= max(2*M+1,M)) {
        S1 ;
      }
    }
  }
}
